import type { Metadata } from 'next';
import Link from 'next/link';
import { Prose } from '@/ui/prose.tsx';
import { DEFAULT_SERVICE_FEE_BPS, BPS_DENOMINATOR } from '@/server/domain/money.ts';

/* Read from the same constant the ledger charges against, so this page cannot
   drift into quoting a rate the platform does not actually apply. */
const RATE_PERCENT = (BigInt(DEFAULT_SERVICE_FEE_BPS) * 100n) / BPS_DENOMINATOR;

export const metadata: Metadata = {
  title: 'Сбор площадки',
  description:
    'Кватэрка берёт 5% с хозяина после завершённой аренды. Никаких платежей за публикацию и никаких комиссий с арендатора.',
};

export default function HostFeesPage() {
  return (
    <Prose
      title="Сбор площадки"
      lede={`Один сбор, одна ставка, один момент начисления: ${RATE_PERCENT}% с хозяина после того, как аренда завершилась и обе стороны это подтвердили.`}
    >
      <h2>Чего нет</h2>
      <ul>
        <li>Платы за публикацию объявления.</li>
        <li>Платы за продвижение в поиске.</li>
        <li>Комиссии с арендатора — он платит только оговорённую с вами сумму.</li>
        <li>Абонентской платы.</li>
      </ul>

      <h2>Когда начисляется</h2>
      <p>
        Сбор начисляется один раз за аренду и только тогда, когда аренда{' '}
        <strong>признана состоявшейся</strong>. Отменённая, истёкшая или не состоявшаяся аренда сбора
        не создаёт.
      </p>
      <p>
        Начисление невозможно продублировать: этому мешают три независимых ограничения в базе данных,
        а не проверка в коде. Повторный запрос вернёт тот же результат, а не второй счёт.
      </p>

      <h2>Как считается</h2>
      <p>
        Сбор — это {String(RATE_PERCENT)}% от суммы аренды за подтверждённый срок. Все суммы
        хранятся в копейках целыми числами, а округление детерминировано, поэтому пересчёт через год
        даст ту же копейку.
      </p>
      <p>
        В разделе{' '}
        <Link href="/dashboard/finance" className="link">
          «Финансы»
        </Link>{' '}
        видно каждое начисление: за какую аренду, от какой базы и по какой ставке.
      </p>

      <h2>Задолженность</h2>
      <p>
        Неоплаченный сбор ограничивает публикацию новых объявлений — но{' '}
        <strong>не трогает уже идущую аренду</strong>. Выселять человека из-за долга хозяина было бы
        наказанием не того, кто должен.
      </p>
      <p>
        Записи о начислениях неизменяемы. Ошибка исправляется не правкой строки, а встречной записью с
        указанием причины и автора — так историю можно проверить, а не только прочитать.
      </p>

      <div className="prose__note">
        <p>
          <strong>Что ещё не готово.</strong> Способа оплатить начисленный сбор внутри площадки пока
          нет: платёжный провайдер не подключён. Сбор рассчитывается и фиксируется, но перевести
          деньги через Кватэрку сейчас нельзя.
        </p>
      </div>

      <p>
        <Link href="/dashboard/listings/new" className="btn btn-primary">
          Разместить квартиру
        </Link>
      </p>
    </Prose>
  );
}
