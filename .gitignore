node_modules/
.next/
out/
dist/
build/
coverage/
*.tsbuildinfo
next-env.d.ts

.env
.env.local
.env.*.local
!.env.example

.pgdata/
*.log
npm-debug.log*

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Uploaded media in local dev
/storage/
.pgdata/
*.tmp.html

# Uploaded media in development. Production uses object storage.
.media/

# Local plugin artifact (redb), not project source.
ruvector.db

# Database dumps. A dump holds every email address, phone number, private
# message and exact flat address in the system — it is as sensitive as the
# database itself and habitually ends up somewhere far less protected.
# See docs/DATABASE_MIGRATION.md §10.
*.dump
*.sql.gz
/backups/
